<p align="center"><a href="https://laravel.com" target="_blank"><img src="https://raw.githubusercontent.com/laravel/art/master/logo-lockup/5%20SVG/2%20CMYK/1%20Full%20Color/laravel-logolockup-cmyk-red.svg" width="400" alt="Laravel Logo"></a></p>

<p align="center">
<a href="https://github.com/laravel/framework/actions"><img src="https://github.com/laravel/framework/workflows/tests/badge.svg" alt="Build Status"></a>

</p>
# projecte
Projecte intermodular 2 de Desenvolupament d'aplicacions WEB
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
nem a crear un CRUD (create, read, update i delete) dins d'un projecte de laravel por poder gestionar les programacions didàctiques de L'IES Sant Vicent Ferrer d'Algemesí.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*Creació de les BBDD**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
er crear les taules fam us del migrate de laravel, per tant entarem a la consola de forma recursiva
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
ocker exec -it laravel-myapp-1 bash
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
 llançarem :
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan make:migration create_ciclos_formativos
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
i volem fer també la taula de programaciones didácticas ho fariem així, pero a aquest projecte no ho gastarem
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan make:migration create_programaciones_didacticas
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
 procedirem a preparar quins camps deu crear en la migració
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
chema::create('ciclos_formativos', function (Blueprint $table) {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->id(); //per defecte ja és PK e autoincremental
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('nombre', 150);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('familia_profesional', 100);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('grado', 50);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('modalidad', 80);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('decreto_referencia', 250);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->boolean('activo');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->timestamps();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       });
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
 per les programacions didactiques:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
chema::create('programaciones_didacticas', function (Blueprint $table) {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->id();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('titulo',100);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('modulo',100);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->foreignId('ciclo_formativo_id')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               ->constrained('ciclos_formativos'); //FK
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('curso_academico', 50);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->string('docente',200);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->integer('horas_totales');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('objetivos');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('contenidos');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('metodologia');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('criterios_evaluacion');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('instrumentos_evaluacion');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('recursos_materiales');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('atencion_diversidad');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->text('observaciones')->nullable(); //crec que es millor que puga ser NULL
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           $table->timestamps();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       });
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
er a que es creen els camps llançarem
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan migrate
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
ra anem a crear el model:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan make:model CicloFormativo -cr
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan make:model ProgramacionDidactica -cr
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
er instal·lar les dependecies de PHP:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
pm install
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
pm install -g npm@11.15.0
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
nstal·lar bootstrap
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
omposer require laravel/ui
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan ui bootstrap 
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
pm install sass --save-dev   
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
na vegada ja tenim preparat el laravel, ja podem treballar amb els moduls que anem a crear.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*Controladors**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
l controlador nes ajuda a fer les funcions sobre els moduls
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
?php
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*modul per gestionar els cicles formatius, permet crear, editar, mostrar i eliminar un cicle formatiu*/
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
amespace App\Http\Controllers;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se App\Models\CicloFormativo;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se Illuminate\Http\Request;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
lass CicloFormativoController extends Controller
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Display a listing of the resource.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function index()
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativos = CicloFormativo::orderBy('nombre')->paginate(5);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return view('ciclosFormativos.index', compact('ciclosFormativos'));
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Show the form for creating a new resource.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function create()
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return view('ciclosFormativos.create');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Store a newly created resource in storage.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function store(Request $request)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       request()->validate(
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'required|min:3|max:150',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'required|min:3|max:100',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'required',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'required',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'required|min:3|max:250',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => 'required'
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ]
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           );
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo = new CicloFormativo();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->nombre= $request->get('nombre');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->familia_profesional= $request->get('familia_profesional');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->grado= $request->get('grado');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->modalidad= $request->get('modalidad');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->decreto_referencia= $request->get('decreto_referencia');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->activo= $request->get('activo');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->save();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return redirect()->route('ciclosFormativos.index');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Display the specified resource.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function show(CicloFormativo $ciclosFormativo)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $CiclosFormativo = $ciclosFormativo;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return view('ciclosFormativos.show', compact('CiclosFormativo'));
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Show the form for editing the specified resource.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function edit(CicloFormativo $ciclosFormativo)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $CiclosFormativo = $ciclosFormativo;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return view('ciclosFormativos.edit', compact('CiclosFormativo'));
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Update the specified resource in storage.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function update(Request $request, CicloFormativo $ciclosFormativo)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->nombre= $request->get('nombre');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->familia_profesional= $request->get('familia_profesional');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->grado= $request->get('grado');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->modalidad= $request->get('modalidad');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->decreto_referencia= $request->get('decreto_referencia');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->activo= $request->get('activo');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->save();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return redirect()->route('ciclosFormativos.index');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Remove the specified resource from storage.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function destroy(CicloFormativo $ciclosFormativo)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativo->delete();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       return redirect()->route('ciclosFormativos.index');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*vistes**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
es vistes principals son:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*index**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
!-- modul per mostrar la liista de ciclesformatius, ens permet editar o borrar un modul-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
extends('template')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('title','Llista de Cicles Formatius')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('content')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <h1>Cicles formatius</h1>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <ul>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       @forelse($ciclosFormativos as $CiclosFormativo)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <li><a href="{{ route('ciclosFormativos.show',$CiclosFormativo)}}">{{$CiclosFormativo->nombre}}
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </a>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <!-- pasem el delete dins de un form per poder passar l'objecte, laravel donaba un error-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <form action="{{ route('ciclosFormativos.destroy',$CiclosFormativo)}}" method="POST">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               @method('DELETE')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               @csrf
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <button type="submit" class="btn btn-danger">Borrar</button>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </form>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <a href="{{ route('ciclosFormativos.edit', $CiclosFormativo) }}" class="btn btn-warning">Editar</a>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </li>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       @empty
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <li>No hi han cicles formatius que mostrar</li>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       @endforelse
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   </ul>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {{$ciclosFormativos->links()}}
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
endsection
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*create**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
!-- En aquesta vista mostrarem el formulari per crear un nou cicle formatiu
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   este modul es basa en el de editar pero en aquest cas no tenim dades a mostrar,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
extends('template')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('title','Formulari nou Cicle formatiu')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('content')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <h1>Crear cicle formatiu</h1>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {{--
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   @if ($errors->any())
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <ul>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @foreach($errors->all() as $error)
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <li>{{ $error }}</li>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endforeach
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </ul>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   --}}
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <form action="{{route('ciclosFormativos.store')}}" method="POST">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       @csrf
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- creació del nom-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="title">Nom del cicle formatiu:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <input type="text" class="form-control" name="nombre" id="nombre" value="{{ old('nombre')}}">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @if($errors->has('nombre'))
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <div class="text-danger">{{$errors->first('nombre')}}</div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- creació de la família professional a la qual pertany el cicle formatiu-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="editorial">Família professional a la qual pertany:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <input type="text" class="form-control" name="familia_profesional" id="familia_profesional" value="{{ old('familia_profesional')}}">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               @if($errors->has('familia_profesional'))
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <div class="text-danger">{{$errors->first('familia_profesional')}}</div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- creació del grau, per evitar error affegim un select entre les 2 opcions que tenim -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="price">Grau:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <select class="form-control" name="grado" id="grado">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Grau Mitjà" {{ old('grado') == 'Grau Mitjà' ? 'selected' : '' }}>Grau Mitjà</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Grau Superior" {{ old('grado') == 'Grau Superior' ? 'selected' : '' }}>Grau Superior</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </select>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @if($errors->has('grado'))
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <div class="text-danger">{{$errors->first('grado')}}</div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- creació de la modalitat, per evitar error affegim un select entre les 2 opcions que tenim -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="author">Modalitat:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
          <select class="form-control" name="modalidad" id="modalidad">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Presencial" {{ old('modalidad') == 'Presencial' ? 'selected' : '' }}>Presencial</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Semipresencial" {{ old('modalidad') == 'Semipresencial' ? 'selected' : '' }}>Semipresencial</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </select>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @if($errors->has('modalidad'))
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <div class="text-danger">{{$errors->first('modalidad')}}</div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- creació de la Referència normativa del títol , per evitar error affegim un select entre les 2 opcions que tenim -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="title">Referència normativa del títol (BOE/DOGV):</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <input type="text" class="form-control" name="decreto_referencia" id="decreto_referencia" value="{{ old('decreto_referencia')}}">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @if($errors->has('decreto_referencia'))
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <div class="text-danger">{{$errors->first('decreto_referencia')}}</div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- creació del actiu, per evitar error affegim un select entre les 2 opcions que tenim,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           es mostra sí o no pero es guarda 1 o 0 al tindre un boolean a les taules -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="author">Actiu:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
          <select class="form-control" name="activo" id="activo">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="1" {{ old('activo') === '1' ? 'selected' : '' }}>SÍ</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="0" {{ old('activo') === '0' ? 'selected' : '' }}>NO</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </select>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @if($errors->has('activo'))
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <div class="text-danger">{{$errors->first('activo')}}</div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           @endif
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <input type="submit" name="Crear" value="Crear" class="btn btn-success btn-block">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   </form>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
endsection
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*show**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
!-- En aquesta vista mostrarem les dades del cicle formatiu que hem seleccionat a la vista index, ens permet eliminar-lo-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
extends('template')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('title','Dades del cicle formatiu')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('content')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <h1>Nom del cicle formatiu: {{$CiclosFormativo->nombre}}</h1>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <p>Família professional a la qual pertany: {{$CiclosFormativo->familia_profesional}}</p>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <p>Grau: {{$CiclosFormativo->grado}}</p>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <p>Modalitat: {{$CiclosFormativo->modalidad}}</p>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <p>Referència normativa del títol (BOE/DOGV): {{$CiclosFormativo->decreto_referencia}}</p>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <p>Actiu: {{ $CiclosFormativo->activo ? 'SÍ' : 'NO' }}</p>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <!-- pasem el delete dins de un form per poder passar l'objecte, laravel donaba un error-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <form action="{{ route('ciclosFormativos.destroy',$CiclosFormativo)}}" method="POST">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               @method('DELETE')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               @csrf
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <button type="submit" class="btn btn-danger">Borrar</button>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </form>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <a href="{{ route('ciclosFormativos.edit', $CiclosFormativo) }}" class="btn btn-warning">Editar</a>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
endsection
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*edit**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
!-- Modul poer poder editar les dades d'un cicle. -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
extends('template')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('title','Formulari editar Cicle formatiu')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('content')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <h1>Editar Cicle Formatiu</h1>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <form action="{{route('ciclosFormativos.update', $CiclosFormativo)}}" method="POST">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       @method('PUT')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       @csrf
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- Nom del cicle formatiu -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="nombre">Nom del cicle formatiu:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <input type="text" class="form-control" name="nombre" id="nombre" value="{{$CiclosFormativo->nombre}}">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- Família professional a la qual pertany -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="familia_profesional">Família professional a la qual pertany:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <input type="text" class="form-control" name="familia_profesional" id="familia_profesional" value="{{$CiclosFormativo->familia_profesional}}">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- Grau, per evitar error affegim un select entre les 2 opcions que tenim -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="price">Grau:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <select class="form-control" name="grado" id="grado">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Grau Mitjà" {{ old('grado', $CiclosFormativo->grado) == 'Grau Mitjà' ? 'selected' : '' }}>Grau Mitjà</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Grau Superior" {{ old('grado', $CiclosFormativo->grado) == 'Grau Superior' ? 'selected' : '' }}>Grau Superior</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </select>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- Modaliat, per evitar error affegim un select entre les 2 opcions que tenim -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="author">Modalitat:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <select class="form-control" name="modalidad" id="modalidad">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Presencial" {{ old('modalidad', $CiclosFormativo->modalidad) == 'Presencial' ? 'selected' : '' }}>Presencial</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="Semipresencial" {{ old('modalidad', $CiclosFormativo->modalidad) == 'Semipresencial' ? 'selected' : '' }}>Semipresencial</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </select>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- Referència normativa del títol -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="decreto_referencia">Referència normativa del títol (BOE/DOGV):</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <input type="text" class="form-control" name="decreto_referencia" id="decreto_referencia" value="{{$CiclosFormativo->decreto_referencia}}">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <!-- Actiu, este select encara que mostra un SI o No, al tindre un boolean a les taules guardem 1 o 0 -->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <div class="form-group">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <label for="decreto_referencia">Actiu:</label>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           <select class="form-control" name="activo" id="activo">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="1" {{ old('activo', $CiclosFormativo->activo) == 1 ? 'selected' : '' }}>SÍ</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               <option value="0" {{ old('activo', $CiclosFormativo->activo) == 0 ? 'selected' : '' }}>NO</option>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           </select>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       </div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       <input type="submit" name="Editar" value="Editar" class="btn btn-warning btn-block">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   </form>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
endsection
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
er al correcte funcionament del programa sobre web.php tenim:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
?php
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se Illuminate\Support\Facades\Route;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se App\Http\Controllers\CicloFormativoController;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se App\Http\Controllers\ProgramacionDidatcticaController;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
oute::get('/', [CicloFormativoController::class, 'index']);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
oute::resource('ciclosFormativos', CicloFormativoController::class);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
om a barra de navegació en totes les pàgines:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
nav class="navbar navbar-expand-lg navbar-dark bg-dark">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
a class="navbar-brand" href="#">Cicles Formatius</a>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
button class="navbar-toggler" type="button" data-toggle="collapse" datatarget="#navbarNav" aria-controls="navbarNav" aria-expanded="false" arialabel="Toggle navigation">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
span class="navbar-toggler-icon"></span>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/button>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
div class="collapse navbar-collapse" id="navbarNav">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
ul class="navbar-nav">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
li class="nav-item">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <a class="nav-link" href="{{ route('ciclosFormativos.index') }}">Llistar Cicles formatius</a>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/li>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
li class="nav-item">
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <a class="nav-link" href="{{ route('ciclosFormativos.create') }}">Crear Cicles formatius</a>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/li>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/ul>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/div>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/nav>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
er personatlar l'error 404
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
extends('template')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('title','Error 404')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
section('content')
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <h1 style="color:tomato;">ERROR 404</h1>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   <p>Pàgina no trobada</p>
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
endsection
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
'estil de la web el tenim en app.scss
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/ Fonts
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
import url('https://fonts.bunny.net/css?family=Nunito');
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/ Variables
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
import 'variables';
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/ Bootstrap
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
import 'bootstrap/scss/bootstrap';
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
onfiguraremels estils que volem aplicar a la nostra web
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
ody
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   font-family: var(--font-sans);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   background-color: #f8f9fa;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   text-align: justify;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
na vegada ja tenim el projecte montat, pero poder probar-ho bé, anem a creader els seeders amb dades "sintétiques", ho farem amb:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan make:seeder CiclesFormatius
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
 al arxiu que ens ha creat l'editem amb:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
?php
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
*
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
reem un sidder amb dades sintétiques per probar les fujncionalitat-->
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
amespace Database\Seeders;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se Illuminate\Database\Console\Seeds\WithoutModelEvents;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se Illuminate\Database\Seeder;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se App\Models\CicloFormativo;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
lass CiclesFormatius extends Seeder
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Run the database seeds.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function run(): void
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       // Crear algunos cicles formatius de ejemplo
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $ciclosFormativos = [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'Desenvolupament d Aplicacions Web',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'Informàtica',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'Grau Superior',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'Semipresencial',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'BOE 666/2026',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => true,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ],
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'Comerç Internacional',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'Comerç',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'Grau Superior',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'Semipresencial',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'BOE 9999/2021',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => true,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ],
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'Eelectricitat',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'eletricitat',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'Grau Mitjà',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'Presencial',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'BOE 1111/2025',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => false,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ],
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'Desenvolupament d Aplicacions Web2',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'Informàtica',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'Grau Superior',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'Presencial',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'BOE 555/2026',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => false,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ],
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'Comerç Internacional2',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'Comerç2',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'Grau Superior',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'Semipresencial',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'BOE 888/2021',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => false,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ],
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           [
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'nombre' => 'Eelectricitat2',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'familia_profesional' => 'eletricitat2',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'grado' => 'Grau Mitjà',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'modalidad' => 'Presencial',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'decreto_referencia' => 'BOE 1112221/2025',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
               'activo' => true,
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           ],
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       ];
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       foreach ($ciclosFormativos as $ciclo) {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           CicloFormativo::create($ciclo);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
ra ja podríem cridar al seeder
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
?php
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
amespace Database\Seeders;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se App\Models\User;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
/ use Illuminate\Database\Console\Seeds\WithoutModelEvents;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
se Illuminate\Database\Seeder;
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
lass DatabaseSeeder extends Seeder
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   /**
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    * Seed the application's database.
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
    */
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   public function run(): void
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   {
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       // User::factory(10)->create();
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       $this->call(CiclesFormatius::class);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       User::factory()->create([
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           'name' => 'Test User',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
           'email' => 'test@example.com',
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
       ]);
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
   }
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.

# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
 ho farem amb:
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
hp artisan migrate:fresh --seed
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
``
# 📚 Sistema de Gestió de Programacions Didàctiques - IES Sant Vicent Ferrer

Projecte intermodular per a l'automatització i gestió de les programacions didàctiques del centre. Aquesta entrega conté la documentació i codi corresponents al **Mòdul B: CRUD de Cicles Formatius**.

---

## 💻 1. Entorn del Sistema Operatiu
L'entorn base utilitzat per al desenvolupament ha estat configurat sobre una màquina virtual amb **Ubuntu 24.04 LTS** corrent sota **VirtualBox**.

---

## 🐳 2. Arquitectura de l'Entorn Virtual (Docker)
Per a aïllar l'entorn de treball i assegurar la replicabilitat de l'aplicació, s'utilitza **Docker** i **Docker Compose**, gestionant tres serveis independents integrats dins d'una mateixa xarxa virtual (`172.21.0.0/16`):

* **Base de Dades (db):** Servidor MySQL 5.7 accessible localment a través del port `3306`.
* **Aplicació Laravel (myapp):** Servidor de producció/desenvolupament exposat a l'exterior a través del port web `8010`.
* **Administrador SQL (phpmyadmin):** Interfície gràfica de gestió web mapejada al port `8005`.

---

## 🛠️ 3. Guia d'Instal·lació i Configuració des de Zero

### Pas 1: Preparació del Dipòsit i instal·lació de Docker en Ubuntu
Instal·la les claus oficials de Docker GPG i munta les fonts APT en el teu sistema base:
```bash
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://docker.com -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://docker.com
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

*Opcional: Si vols executar Docker sense necessitat de fer `sudo`, afegeix el teu usuari al grup de Docker:*
```bash
sudo groupadd docker
sudo usermod -aG docker $USER
```
*(Es requereix reiniciar el sistema per a aplicar el canvi d'usuari).*

### Pas 2: Aixecar el Contenidor i Enllaç de Seguretat de MySQL
1. Executa la instal·lació dels contenidors des de l'arrel on resideix el fitxer `docker-compose.yml`:
   ```bash
   docker compose up -d
   ```
2. Un cop iniciat, s'ha de configurar l'usuari amb privilegis d'accés dins del contenidor de la base de dades. Obre una terminal interactiva:
   ```bash
   docker exec -it laravel-db-1 bash
   mysql -u root -p
   ```
   *(Prem `Enter` quan et demani contrasenya, ja que per defecte està configurada com a buida).*

3. Executa les següents sentències SQL per atorgar els permisos complets del node:
   ```sql
   CREATE USER 'jose'@'172.21.0.3' IDENTIFIED BY '';
   GRANT ALL PRIVILEGES ON *.* TO 'jose'@'172.21.0.3';
   FLUSH PRIVILEGES;
   exit
   ```

### Pas 3: Sincronització de l'Entorn Laravel (`.env`)
Configura el fitxer de variables d'entorn `.env` situat a l'arrel de la teva aplicació amb els paràmetres de connexió del contenidor Docker:
```env
DB_CONNECTION=mysql
DB_HOST=172.21.0.2
DB_PORT=3306
DB_DATABASE=BD_myapp
DB_USERNAME=jose
DB_PASSWORD=
```
Dins de l'arxiu `config/database.php` assegura't que s'utilitza la connexió per defecte:
```php
'default' => env('DB_CONNECTION', 'mysql'),
```

### Pas 4: Estructuració de Dades i Dependències Front-end
Accedeix a la terminal del contenidor de l'aplicació web per a generar les estructures físiques i dependències de Bootstrap:
```bash
docker exec -it laravel-myapp-1 bash

# Executar migracions i alimentar dades amb seeders
php artisan migrate:fresh --seed

# Instal·lar paquets de NodeJS i Bootstrap 5
npm install
composer require laravel/ui
php artisan ui bootstrap
npm install sass --save-dev

# Compilar els recursos estàtics del projecte (Obligatori per a visualitzar estils)
npm run build
```

---

## 🗺️ 4. Estructura de la Base de Dades i Taules

L'estructura física de la taula `ciclos_formativos` creada a la base de dades a través de les migracions de Laravel és la següent:

* **`id`**: INT (Primary Key, Autoincremental).
* **`nombre`**: VARCHAR(150) - Nom descriptiu del cicle formatiu.
* **`familia_profesional`**: VARCHAR(100) - Àrea a la qual pertany.
* **`grado`**: VARCHAR(50) - Grau Mitjà / Grau Superior (Controlat per llista selectiva).
* **`modalidad`**: VARCHAR(80) - Presencial / Semipresencial.
* **`decreto_referencia`**: VARCHAR(250) - Marc legal DOGV/BOE.
* **`activo`**: BOOLEAN - Indica l'estat actiu al centre (Gestionat gràficament amb Checkbox).

---

## 🗺️ 5. Taula de Rutes i Operacions RESTful (CRUD)


| Mètode HTTP | URI | Acció de la Ruta | Descripció Operativa |
|---|---|---|---|
| **GET** | `/ciclosFormativos` | `ciclosFormativos.index` | Visualitza el llistat amb paginació de 5 registres i barra de cerca. |
| **GET** | `/ciclosFormativos/create` | `ciclosFormativos.create` | Mostra el formulari de creació del cicle. |
| **POST** | `/ciclosFormativos` | `ciclosFormativos.store` | Emmagatzema el cicle des del Form Request. |
| **GET** | `/ciclosFormativos/{id}` | `ciclosFormativos.show` | Desplega la fitxa sencera de dades del cicle seleccionat. |
| **GET** | `/ciclosFormativos/{id}/edit` | `ciclosFormativos.edit` | Carrega el formulari d'edició amb dades existents. |
| **PUT** | `/ciclosFormativos/{id}` | `ciclosFormativos.update` | Desa els canvis realitzats prèvia validació de dades. |
| **DELETE** | `/ciclosFormativos/{id}` | `ciclosFormativos.destroy` | Elimina permanentment el registre de la base de dades. |

---

## ⚡ 6. Disseny de Qualitat: Validacions amb Form Request
Sota l'arquitectura i bones pràctiques de desenvolupament, les regles de validació es gestionen en una capa de control externa mitjançant la comanda `php artisan make:request CicloFormativoRequest`.

D'aquesta manera, el controlador no conté lògica de validació encastada en els mètodes `store` o `update`, la qual cosa garanteix la seguretat en els llindars de longitud i tipus de dades dels camps (`nombre`, `familia_profesional`, `decreto_referencia`), oferint alertes visuals gràfiques en color vermell utilitzant la directiva `@error` de Bootstrap 5 en el cas que un camp estigui buit o incomplet.

---

## 🔌 7. Complements del Workspace recomanats (VS Code)
Per a garantir la continuïtat del projecte per un altre tècnic o programador, es recomana l'ús de la versió de Visual Studio Code per a Linux amb les següents extensions del marketplace actives:
* **Docker Extension** (Microsoft)
* **Laravel Extension Pack** (Winnie Lin) - Ofereix auto-completat intel·ligent del Framework Laravel, Blade i Artisan.
